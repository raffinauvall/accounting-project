import { Plus, UserRound } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { DemoAction } from "@/components/demo-action";
import { createUserAction, updateUserPasswordAction } from "@/app/(app)/users/actions";
import { listUsers } from "@/server/services/user.service";

const demoUsers = [{ id: "admin", name: "Administrator", email: "admin@example.com", role: "ADMIN", isActive: true }, { id: "finance", name: "Keuangan Demo", email: "finance@example.com", role: "FINANCE", isActive: true }, { id: "viewer", name: "Penampil Demo", email: "viewer@example.com", role: "VIEWER", isActive: true }];
const roleLabels: Record<string, string> = { ADMIN: "Admin", FINANCE: "Keuangan", VIEWER: "Penampil" };

export default async function UsersPage() {
  const liveUsers = await listUsers().catch(() => null);
  const users = liveUsers ?? demoUsers;
  const isDemo = !liveUsers;
  return <AppShell title="Pengguna"><div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><div className="label text-[#111111]">Akses aplikasi</div><h2 className="mt-2 text-3xl font-bold tracking-tight">Pengguna</h2><p className="mt-2 text-sm text-[#6b7785]">Buat pengguna, atur peran, dan ganti kata sandi pengguna.</p></div>{isDemo ? <DemoAction className="flex h-10 items-center justify-center gap-2 rounded-md bg-[#111111] px-4 text-sm font-semibold text-white"><Plus size={16} />Tambah pengguna</DemoAction> : <span className="rounded-md bg-[#f0f0f0] px-3 py-2 text-xs font-semibold text-[#2b2b2b]">Database tersambung</span>}</div>{!isDemo && <form action={createUserAction} className="panel grid gap-4 p-6 md:grid-cols-2 xl:grid-cols-4"><div className="md:col-span-2 xl:col-span-4"><h3 className="font-bold">Tambah pengguna</h3><p className="mt-1 text-sm text-[#6b7785]">Kata sandi minimal 8 karakter dan akan disimpan dalam bentuk hash.</p></div><label className="label">Nama<input name="name" required maxLength={150} className="mt-2 h-10 w-full rounded-md border border-[#dce2e7] px-3 text-sm" placeholder="Nama lengkap" /></label><label className="label">Email<input name="email" type="email" required className="mt-2 h-10 w-full rounded-md border border-[#dce2e7] px-3 text-sm" placeholder="user@contoh.com" /></label><label className="label">Kata sandi<input name="password" type="password" minLength={8} required className="mt-2 h-10 w-full rounded-md border border-[#dce2e7] px-3 text-sm" placeholder="Minimal 8 karakter" /></label><label className="label">Peran<select name="role" defaultValue="VIEWER" className="mt-2 h-10 w-full rounded-md border border-[#dce2e7] bg-white px-3 text-sm"><option value="ADMIN">Admin</option><option value="FINANCE">Keuangan</option><option value="VIEWER">Penampil</option></select></label><button className="flex h-10 items-center justify-center gap-2 rounded-md bg-[#111111] px-4 text-sm font-bold text-white md:col-span-2 xl:col-span-4"><Plus size={16} />Simpan pengguna</button></form>}<div className="panel overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#fbfcfd] text-xs uppercase tracking-wider text-[#7e8b97]"><tr><th className="px-6 py-4">Pengguna</th><th className="px-6 py-4">Peran</th><th className="px-6 py-4">Status</th><th className="px-6 py-4">Kata sandi</th></tr></thead><tbody className="divide-y divide-[#eef1f3]">{users.map((user) => <tr key={user.id}><td className="px-6 py-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#f0f0f0] text-[#2b2b2b]"><UserRound size={16} /></div><div><div className="font-semibold">{user.name}</div><div className="text-xs text-[#8a96a3]">{user.email}</div></div></div></td><td className="px-6 py-4"><span className="rounded-full bg-[#edf0f2] px-2.5 py-1 text-[11px] font-bold text-[#5f6d7a]">{roleLabels[user.role] ?? user.role}</span></td><td className="px-6 py-4 text-[#2b2b2b]">{user.isActive ? "Aktif" : "Nonaktif"}</td><td className="px-6 py-4">{isDemo ? <span className="text-xs text-[#a3adb7]">Demo</span> : <form action={updateUserPasswordAction} className="flex items-center gap-2"><input type="hidden" name="id" value={user.id} /><input name="password" type="password" minLength={8} required placeholder="Kata sandi baru" className="h-8 w-40 rounded border border-[#dce2e7] px-2 text-xs" /><button className="rounded bg-[#f0f0f0] px-2 py-1 text-xs font-bold text-[#2b2b2b]">Ganti</button></form>}</td></tr>)}</tbody></table></div></div></div></AppShell>;
}
