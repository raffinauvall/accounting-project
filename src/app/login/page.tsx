import Image from "next/image";
import logo from "@/logo.jpg";
import { loginAction } from "@/app/login/actions";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const params = await searchParams; return <main className="grid min-h-screen place-items-center bg-[#f7f9fb] p-5"><section className="panel w-full max-w-md p-8"><div className="mb-8 flex items-center gap-3"><div className="grid h-10 w-10 overflow-hidden rounded-lg bg-[#111111]"><Image src={logo} alt="Logo PT. APST" width={40} height={40} className="h-full w-full object-cover" /></div><div><div className="font-bold">PT. APST</div><div className="text-xs text-[#8a96a3]">Finance workspace</div></div></div><h1 className="text-2xl font-bold">Masuk ke workspace</h1><p className="mt-2 text-sm text-[#6b7785]">Masukkan email dan password untuk melanjutkan.</p>{params.error && <p role="alert" className="mt-4 rounded-md bg-[#fff0ef] p-3 text-sm text-[#b13f37]">{params.error}</p>}<form action={loginAction} className="mt-7 space-y-4"><label className="label block">Email<input name="email" type="email" defaultValue="admin@example.com" required className="mt-2 h-11 w-full rounded-md border border-[#dce2e7] px-3 text-sm" /></label><label className="label block">Password<input name="password" type="password" defaultValue="development-only-change-me" required className="mt-2 h-11 w-full rounded-md border border-[#dce2e7] px-3 text-sm" /></label><button className="flex h-11 w-full items-center justify-center rounded-md bg-[#111111] text-sm font-bold text-white">Masuk</button></form></section></main>; }
